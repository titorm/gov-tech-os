import { municipios } from '../schemas/municipios';
import { usersToMunicipios } from '../schemas/usersToMunicipios';

// Tests intentionally left empty — this package uses tsc-only compile checks.
